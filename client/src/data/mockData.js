/**
 * Centralized Operational Datasets for Planet Maritime Intelligence
 * ─────────────────────────────────────────────────────────────────
 * Configured with empty datasets ready for real backend API consumption:
 *   • GET /api/spills
 *   • GET /api/analysis
 *   • GET /api/reports
 *   • GET /api/alerts
 *   • GET /api/users (Admin only)
 */

export const mockSpillEvents = [];
export const mockIntelligence = [];
export const mockAnalysisQueue = [];
export const mockReports = [];
export const mockAlerts = [];
export const mockUsers = [];
